# 开发笔记（给 Codex / 新对话交接用）

## 工程与构建

- 工程根目录：`D:\塔2mod制作\Aristocrat`（Godot 4.5.1 Mono 工程，modid = `Aristocrat`）
- 游戏：`D:\Steam\steamapps\common\Slay the Spire 2`，日志 `C:\Users\V5000D\AppData\Roaming\SlayTheSpire2\logs\godot.log`（可以直接读）
- 参考源码在工程外：`D:\塔2mod制作\refs\`（游戏反编译 `sts2_src`、一代原 mod `TheAristocrat_src`、`BaseLib_src`、`Watcher`、`Downfall2` 等）
- 构建：`dotnet build`（自动复制 dll+json 到游戏 mods）→ `dotnet build -t:ExportPck`（导出并清理 pck）
- 导出偶发报 `Cannot set object script`，重跑一次即可

## 关键约定

- **类名 = 模型 ID**（Slugify 类名），所有卡牌类必须带 `Aristocrat` 前缀，否则会和本体撞车
- 卡图放 `Aristocrat/images/cards/Aristocrat<名字>.png`；能力图标放 `Aristocrat/images/powers/<能力类名>.png`（84px 那张）
- **游戏原始路径**的资源要放工程顶层 `images/` 下（例如 `images/packed/sprite_fonts/aristocrat_energy_icon.png`），放 `Aristocrat/images/` 下 res:// 路径会多一层前缀
- pck 里不能带 `project.binary` / `.godot/uid_cache.bin`，`tools/repack-pck.ps1` 会自动剔除
- 本地化：`Aristocrat/localization/{zhs,eng}/{cards,powers,relics,combat_messages}.json`；文件名就是表名
- `{IfUpgraded:show:升级后|升级前}`；固有/保留/消耗等关键词不要写进描述文本（会自动渲染）
- 卡的控制台 ID = 类名大写加下划线，例如 `card ARISTOCRAT_STRIKE deck`

## 已经踩过的坑（别再踩）

1. mod 角色必须打补丁插进 `ModelDb.get_AllCharacters`（本体写死了 5 个）
2. 遗物必须注册进某个遗物池，否则选人时抛 `Sequence contains no matching element`
3. **本体硬编码角色表共三处**，mod 角色打赢精英/首领都会抛异常导致卡死：`CheckFifteenElitesDefeatedEpoch`、`CheckFifteenBossesDefeatedEpoch`、`ObtainCharUnlockEpoch`——都已在 `ModdedCharacterProgressPatch` 里跳过
4. `CardModel.Owner` 的 getter 会断言实例可变，对"规范模型"（卡牌总览、奖励预览）读取会抛 `CanonicalModelException`
5. 金币只有"获得"有钩子（且不发给战斗中的能力），"失去"完全没有——虚荣靠 `Patches/GoldChangedPatch` 挂在 `PlayerCmd` 上
6. 战斗是一整条 await 链，从 setter 里甩出去的任务接不进去，必须用 `Task.WhenAll` 并回调用方
7. **卡牌总览那一栏应该由 BaseLib 自动生成**，但它只认继承 `CustomCharacterModel` 的角色（见"下一步"）
8. **关键词别写死在牌上**：「上流社交界」「冷静一击」这种"让打击/防御获得消耗"的能力要用
   `TryModifyKeywordsInCombat` 动态给（本体的「命运之线」`HexPower` 给牌加虚无就是这么写的）。
   写成 `CardCmd.ApplyKeyword` 有两个坑：新生成 / 变化出来的牌（往昔石像的究极打击、天命塞的究极防御）漏掉消耗；
   而且能力到期后关键词还留在那张牌上，整场战斗都变消耗。
   反过来说，"变化 / 复制牌"时要记得调 `AristocratCard.InheritCardModifiers` 把原牌的关键词和附魔抄过去。
9. **别自己往 `SerializableCard.Props` 里塞新名字**（第一版永久固有就是这么翻车的）：
   走 JSON 存档时 `SavedProperties.Fill` 对没见过的名字是**静默跳过**，看着没问题；
   但联机同步和**战斗回放**走的是二进制 `SavedProperties.Serialize`，那里每个属性名都要先注册成 net ID，
   没注册会直接抛 `ArgumentException: SavedProperty name XXX could not be mapped to any net ID!`。
   具体表现：战斗结束（`CombatManager.EndCombat` 里写 replay）和快速 SL 重启房间（`RunManager.CleanUp` 写 replay）
   都会断在这条 await 链上 → 战斗卡死 / 退回主菜单。
   **正确做法**：要往本体模型上加存档字段，用 BaseLib 的 `SavedSpireField<TKey,TVal>`（见下），
   或者在 mod 自己的模型上声明 `[SavedProperty]` 属性——BaseLib 会把名字注册进 net-id 表并同步调整 `NetIdBitSize`。

## 当前进度

- 卡牌：一代 80 个卡牌类**全部搬完**（卡池 78 张，含自制先古卡 强制征税 / 祖训）
- 遗物：**11 / 11 全部搬完**（家族纹章、家族荣誉、白金盾、往昔石像、贵族印记、黑钻石、黄金冠、钱币盒、瓶装混沌、空白支票、宝库钥匙），
  商店机制（首战替换商店 / 精英战后开商店 / 八折）已完成且支持存读档
- 特典系统（自定义关键词 + 统一判定 `Perk/PerkSystem` + 购买钩子）已完成；
  宝库钥匙的"你的稀有牌都带特典：获得一件随机遗物"按塔1 的 `hasVaultKeyPerk` 语义做进了这套判定里
  （卡面会写出效果文字，买下后牌组里也显示、SL 不掉，天生支配者之类也能触发它）
- 先古之民接好了奥罗巴斯（初始遗物/卡牌先古升级）、达夫（尘封魔典 + 贵族的两个 Boss 级遗物）
- 未做：多人联机未测、战斗模型仍借用铁甲战士、卡牌总览还没有贵族自己的筛选栏

## 下一步（待办）

1. 卡牌总览给贵族加一栏：**不要**改基类（试过了，见下），要照 BaseLib `GenerateCustomFilters` 的思路自己复制筛选按钮节点、插到正确位置（BaseLib 是把自定义角色插在本体机器人前面），且只注册贵族自己的两张表
2. 多人联机未测（战斗模型仍借用铁甲战士；本地只能单人起局）
3. 可选：给宝库钥匙那行卡面文字加"悬停出关键词提示"（现在是纯文本；需要的话复用 `SavedSpireField` 给牌挂持久关键词标记）
4. GitHub 推送仍有 403（凭据权限问题），未解决

已完成的旧待办：11 个遗物、肖像的永久固有（`SavedSpireField` 存档）、宝库钥匙的稀有牌特典、纪元审计。

### 已否决的路子（别重试）

把 `Character/Aristocrat.cs` 改成继承 BaseLib 的 `CustomCharacterModel`——编译能过，但运行时全崩：
所有 mod 的卡牌总览失效、贵族选人背景消失、名字显示成代码、进游戏直接崩。
原因是 BaseLib 那套基类自带角色登记/本地化/卡池的完整约定，我们只换基类、其余仍走老路，两边对不上。
要真用就得把角色、卡池、本地化、注册整套迁进去，是一次大改造。

### 遗物实现备忘（11 个都在 `Scripts/Relics/`）

原图直接从 `D:\塔2mod制作\3719013006\TheAristocrat.jar` 里抽的
（`aristocratResources/images/relics/<名字>.png` + `<名字>Outline.png`），
文案取自同 jar 的 `aristocratResources/localization/{zhs,eng}/Relicstrings.json`，都是 128px 小图标，不用改尺寸。

| 类 | 塔1 定位 → 塔2 稀有度 | 实现要点 |
| --- | --- | --- |
| FamilyCrest / FamilyHonor | 起始 / 先古 | 商店机制在 `Shop/`，荣誉多了八折 |
| PlatinumShield | 罕见 | `ModifyBlockAdditive`：自己打出的[防御]额外 +3 格挡，条件照抄 `DexterityPower` |
| OldStatue | 稀有 | `AfterPlayerTurnStart` + `PlayerCombatState.TurnNumber == 4`，把三堆里的[打击]用 `CardCmd.Transform` 变[究极打击]，升级等级一起带过去 |
| NobleMark | Boss → 先古 | `ModifyMaxEnergy +1`（塔1 是 energyMaster）；`AfterObtained` 里 `CardSelectCmd.FromDeckGeneric` 选最多 3 张打击/防御，`RunState.CloneCard` 各复制一份 |
| BlackDiamond | Boss → 先古 | `AfterObtained` 升级全部打击/防御；`AfterRoomEntered` 认 `RestSiteRoom`，随机给 1 张究极打击/防御。用 `[SavedProperty] LastGrantedRestFloor` 防读档重复发 |
| GoldCirclet | 商店 | 战斗开始随机 +2 力量/敏捷（照 `Vajra` 的 `AfterRoomEntered(CombatRoom)`）。"能反复出现"按塔1 的 `GoldCircletRewardPatch` 做：身上每有一顶 +5%（上限 50%）把别的**卡牌奖励 / 遗物奖励**（`TryModifyRewardsLate`，这样转经轮额外加的那栏也能被替换）和**商店遗物**（`Patches/GoldCircletRewardPatch` 挂抽取池）换成黄金冠，本体「头环」出现时必换；`IsStackable` 保险 |
| CoinBox | 稀有 | 拾取给 1 张古钱币 + 计数 2。奖励条目走 `TryModifyRewards`（界面上直接显示古钱币），商店/事件等入口走 `Patches/CoinBoxRelicPatch`（前缀改 `RelicCmd.Obtain` 的 `ref relic` 参数） |
| BottledChaos | 罕见 | `[SavedProperty] SerializableCard` 存瓶中的牌（塔2 没有 uuid，先按 ID+升级等级匹配，找不到再只按 ID）；`BeforeHandDrawLate` 里 `DrawPile.MoveToTopInternal` → 起手必抽到 |
| BlankCheck | 普通 | 挂 `TryModifyCardRewardAlternatives`（"跳过/献祭"那排按钮），加一个"金币 +15"；注意原版上限是 2 个额外选项，满了就不加 |
| VaultKey | 稀有 | "在商店买下的稀有牌带特典：获得一件随机遗物"。范围是**买下来那一张**：商店摆牌时 `VaultKey.ModifyMerchantCardCreationResults` 给稀有牌实例打标记（`Perk/VaultKeyPerkTracker`，BaseLib 的 SavedSpireField → 跟着牌存档，卖后牌组里也有、SL 不掉；复制牌靠 CopyOnClone 也能带过去），买下时触发一次；之后判定/触发统一走 `Perk/PerkSystem`（天生支配者、赠品、奢侈品、纯血、族谱、加冕都认它），发遗物是 `VaultKey.GrantRelic`；卡面那行说明由 `Patches/VaultKeyPerkTextPatch` 追加 |

### 「特典」的统一判定（PerkSystem）

塔1 的 `PerkHelper` 里"是不是特典牌"是**两件事的并集**：卡自带的特典，或者宝库钥匙标记，
而 `trigger(card)` 里这两条都会触发。我们的塔2 版原来只在商店购买那一处处理宝库钥匙，
于是「天生支配者」「赠品」「奢侈品」「纯血」「族谱」「加冕」这些认"特典牌"的效果全都漏了它。

**范围要注意**：宝库钥匙那条只给"在商店里买下来的那张稀有牌"，
不能按"稀有度 + 你有钥匙"推导（那样连奖励/事件里拿到的稀有牌都会带上）。
所以它是**牌身上的持久标记**（`Perk/VaultKeyPerkTracker`，`SavedSpireField<CardModel, bool>`，
和肖像固有同一套），商店摆牌时打上、跟着牌存档。

现在统一到 `Perk/PerkSystem.cs`：

- `HasPerk(card)` = `card is IPerkCard || HasVaultKeyPerk(card)`（`AristocratCard.IsPerkCard` 也转发到这里）；
- `HasVaultKeyPerk(card)` = 牌身上有没有 `VaultKeyPerkTracker` 的标记（= 这张牌是带钥匙在商店买的）；
- `TriggerPerk(card, owner)` = 卡自己的 `OnPerkTriggered` + 宝库钥匙的 `VaultKey.GrantRelic`（两条可能同时触发）；
- 触发点：`Perk/PerkPurchasePatch`（商店购买）、`NextPerkTriggersPower`（天生支配者）、`FreeGiftPower`；
  判定点：`LuxuryGoods` / `PureBlood` / `FamilyTree` / `Coronation`。

**卡面文字**：塔2 的卡文是 `CardModel.GetDescriptionForPile` 拼出来的，关键词行只有"[gold]特典[/gold]。"
（`CardKeywordExtensions.GetCardText` 只给标题，这也是"只有特典两个字"的原因）。
所以 `Patches/VaultKeyPerkTextPatch.cs` 挂在这个方法的**私有重载**后面，给带标记的牌补一行
`relics.VAULT_KEY.perkText`（"[gold]特典[/gold]：获得一件随机遗物。"）。挂私有重载是因为公开重载和升级预览
最后都会走它，一处就能覆盖商店 / 牌组 / 手牌 / 战斗 / 升级预览。
标记跟着牌存档，所以**买下来之后牌组里照样显示、SL 也不会掉**；
商店那边原来那条"给商店卡实例加[特典]关键词"的写法已经删掉，否则会多出一行光秃秃的"特典。"。
（`VaultKeyPerkTracker` 只管一个布尔值、不碰关键词，所以读档 / 降级都不需要补丁，
`CopyOnClone` 让多利之镜复制、战斗内克隆、蛋系遗物改牌都保住这个标记。）

顺带修了一处抄漏：加冕（Coronation）塔1 的判定是 `isStrikeOrDefend || isInnate || hasPerk`，
我们之前漏了特典牌，卡文也少了"特典牌"，现在都补上了。

要验证：有宝库钥匙时——商店稀有牌卡面有"特典：获得一件随机遗物。"、买下后牌组里也显示、SL 之后不掉；
打出「天生支配者」后再打**那张买来的**牌，战斗里直接拿到遗物、出战斗还在；
而奖励/事件里拿到的稀有牌（没在商店买过）不该显示这行、也不该触发特典。

### 永久关键词的存档（肖像特典给的固有）

塔2 的卡牌存档 `SerializableCard` 里**没有关键词这一项**，关键词只活在内存（`CardModel._keywords`）里，
所以 `AddKeyword` 加出来的"永久"效果一存一读就没了，得自己存一个"这张牌要一直带着它"的标记。

**标记怎么做才是对的**（第一版踩了"坑"第 9 条，别重复）：

- `Perk/PersistentKeywordTracker.cs` 用 BaseLib 的 `SavedSpireField<CardModel, bool>`：

  ```csharp
  internal static readonly SavedSpireField<CardModel, bool> PersistentInnate =
      new(() => false, "AristocratPersistentInnate");   // 存档里的名字是 CardModel_AristocratPersistentInnate
  static PersistentKeywordTracker() { PersistentInnate.CopyOnClone(); }  // 复制牌时跟着走
  ```

  BaseLib 在"mod 后置初始化"时会扫 mod 里的静态 `SavedSpireField` 字段，把它注册进
  `SavedPropertiesTypeCache`（名字→net ID）并调好 `NetIdBitSize`，JSON 导出/导入也由它接管；
  `Entry.Init()` 里额外碰一下这个字段，保证构造（=登记）发生在 BaseLib 那次注册之前。
- `Patches/PersistentKeywordPatches.cs` 只剩两个补丁（其余的 BaseLib 都管了）：
  `CardModel.FromSerializable` 读档后把**关键词**补回来（关键词本身不在存档里），
  `CardModel.DowngradeInternal` 兜住"临时升级结束会把 `_keywords` 重建"这条。
- 复制牌靠 `CopyOnClone()`（走 `AbstractModel.MutableClone`），变化牌靠
  `AristocratCard.InheritCardModifiers` 里的 `CarryOverInnate`。
- 旧存档兼容：第一版写在 `Props.ints` 里的 `SavedAristocratPortraitInnateMarker` 现在只读不写，
  读到就当成永久固有，下次存档以新字段写回去。

要验证：贵族买肖像 → 特典选一张牌 → 该牌显示[固有] → **存档退出再进** → 牌上还有[固有]、战斗中起手必抽到；
再确认**战斗正常结束**（结束时会写 replays/latest.mcr）和**快速 SL 重启房间**都不再异常；
顺带试试多利之镜复制那张牌、以及临时升级类的效果（降级后固有不该消失）。

### 遗物池（重要）

- 贵族有自己的遗物池 `Character/AristocratRelicPool.cs`（观者 mod 的 `WatcherRelicPool` 同一套做法），
  `Aristocrat.RelicPool` 指向它。`RelicGrabBag.Populate` 会自己把 `SharedRelicPool` 拼上，所以共享遗物照常能出，
  贵族专属遗物不会漏给别的角色——**不要再**用 `ModHelper.AddModelToPool<SharedRelicPool, ...>` 注册（那会让所有角色都刷到贵族的遗物）。
- 塔1 的 BOSS 级（贵族印记、黑钻石）在塔2 对应**先古级**：先古级不在随机池里，塔2 的 Boss 遗物来源是先古之民「达夫」写死的 `_validRelicSets` 私有表，
  所以 `Patches/DarvAncientRelicPatch.cs` 反射往那张表里补了一组（限定只有贵族能抽到）。用**前缀**注入，
  因为选项是进事件时一次算完的，得赶在 `GenerateInitialOptions` 之前补表，第一次遇到达夫才有效。
  `ValidRelicSet` 是私有嵌套 struct，只能 `Activator.CreateInstance` 构造；整段包在 try/catch 里，失败只记日志。

### 纪元（时间线）/ 进度系统审计

本体的纪元与进度存档里有一堆"按角色写死"的分支，mod 角色踩中会**抛异常卡住流程**。逐个查过的结果：

| 位置 | 触发时机 | 原表现 | 处理 |
| --- | --- | --- | --- |
| `ProgressSaveManager.CheckFifteenElitesDefeatedEpoch` | 打赢精英 | `throw ArgumentOutOfRangeException` | `Patches/ModdedCharacterProgressPatch` 跳过 |
| `ProgressSaveManager.CheckFifteenBossesDefeatedEpoch` | 打赢首领 | 同上 | 同上 |
| `ProgressSaveManager.ObtainCharUnlockEpoch` | 打赢首领 | `EpochModel.Get("XXX2_EPOCH")` 抛 `ArgumentException` | 同上 |
| 奥罗巴斯事件的「海玻璃」选项 | 进入奥罗巴斯事件 | `SeaGlass.Title` = `relics` 表的 `SEA_GLASS.<角色ID>.title`，本体只给了 5 个角色；缺键时 `LocTable.GetRawText` **抛 LocException**，事件选项渲染就炸 → 事件卡死 | 本地化补 `SEA_GLASS.ARISTOCRAT.title`（黄金玻璃 / Golden Glass） |

已确认安全（不用管）：

- 局末结算的 `PostRunUnlockCharacterEpochCheck` / `CheckAscensionOneCompleted` 对未知角色返回 null 且都有判空，只是拿不到本体角色的 1 号 / 7 号纪元。
- `SunkenTreasury`（`.goldMonologue`）和 `AromaOfChaos`（`.aromaPrinciple`）也是缺键就抛 LocException 的类型——我们的 `characters.json` 早就有这两个键；本轮把唯一缺的 `unlockText` 也补上了（只在角色被锁住时才用，而贵族永远在解锁列表里）。
- 先古对话：`AncientDialogueSet` 没有该角色的条目时回落到 `AgnosticDialogues`（通用台词），不会抛。
- `YummyCookie` 的曲奇图案、运行历史里的小/大攻击音效、遗物总览分类、角色统计界面：都有默认分支，最坏是贵族拿到铁甲战士的曲奇图（纯装饰）。
- 成就：都是按条件触发，角色专属的那几个不会误触发。
- 结算界面 `NGameOverScreen.SaveBadgesToProgress` 里的 `Progress.CharacterStats[角色]` 索引器：选框时 `StartRunLobby.SetSingleplayerAscensionAfterCharacterChanged` 会 `GetOrCreateCharacterStats`，所以贵族一定有条目。
- `StartRunLobby.IsAscensionEpochRevealed` 对未知角色直接 `return true`，不会抛。
- 时间线界面本身是按"已获得的纪元"渲染的，贵族没有纪元 → 没有可渲染内容。

已知"不卡但会变"的副作用（都没改）：

- `UpdateEpochsPostRun` 里 `OROBAS_EPOCH` / `DAILY_RUN_EPOCH` 的条件是 `Progress.CharacterStats.Count >= ModelDb.AllCharacters.Count()`，
  而贵族被注册进了 `AllCharacters` → 这两个纪元现在多一个"用贵族打一局"的要求（装了别的角色 mod 就更多）。
- `SaveManager.GetAggregateAscensionCount()` = `AllCharacters.Count() * 10`，总进阶进度的分母每多一个 mod 角色 +10
  （等于多一个能刷的角色，不是问题）。
- **进阶不用为 mod 角色做任何事**（玩家实测确认：进阶是通用内容，会自动适配所有角色）：
  `IncrementSingleplayerAscension` 只看"单机通关时 `run.Ascension == charStats.MaxAscension`"，跟角色无关；
  `StartRunLobby.IsAscensionEpochRevealed` 对未知角色直接 `return true`（本体给 mod 角色留的口子），
  所以贵族通关一局后 `MaxAscension` 就照常从 0 往上涨，进阶层级与本体角色共用。

### 本轮没做成的验证

- 只验证到：`dotnet build` + `dotnet build -t:ExportPck` 通过、pck 里含 9 组新图标（png.import + ctex）与 `card_reward_ui.json`，
  以及脚本自检"每个遗物类的 title/description/flavor/selectionScreenPrompt 本地化键 + 图标文件 + 池注册"都齐全。
- 这台机器上无法驱动游戏 UI（Computer Use 的原生应用接口被禁用），也没找到命令行起局的入口，所以**没有游戏内实测**。
  下次接手请先：进贵族跑一局，控制台 `relic PLATINUM_SHIELD` 之类逐个加，重点看 `C:\Users\V5000D\AppData\Roaming\SlayTheSpire2\logs\godot.log` 有没有异常。
- 玩家反馈后改的两处（同样只做了编译验证）：
  1. 上流社交界 / 冷静一击给[消耗]改成动态关键词，并且变化牌时继承关键词与附魔（见"坑"第 8 条）；
  2. 黄金冠"能反复出现"改成塔1 的奖励替换逻辑（见遗物表的 GoldCirclet 行），原来那版"把自己塞回抽取池"的做法已删掉。
- 玩家实测：上面两处 + 奥罗巴斯海玻璃 + 11 个遗物都正常；进阶无需额外处理（见上一节）。
- 肖像的永久固有：第一版自己往 `Props.ints` 塞名字，**炸了**（战斗结束写 replay、快速 SL 重启房间都会抛
  "could not be mapped to any net ID" → 战斗卡死 / 退回主菜单）；已改成 BaseLib 的 `SavedSpireField`
  （见「永久关键词的存档」一节），玩家实测通过：存读档后固有还在、快速 SL 和战斗结算都正常。
- 宝库钥匙改成"商店买下的稀有牌才带特典"（见「特典的统一判定」一节），玩家实测通过。
